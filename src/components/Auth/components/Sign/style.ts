import { css } from "../../../../../stitches.config";

export const styles = {
  'sign': css({
    position: 'absolute', 
    zIndex: -1, 
    width: '100%', 
    textAlign: 'center', 
    top: '28%',
    fontSize: "20",
    fill: "#fff",
    fontFamily: '$blak',
    opacity: .75
  }),
}